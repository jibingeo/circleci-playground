machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker version

test:
  override:
    - echo hello

deployment:
  production:
    branch: production
    commands:
      - echo "Hello production"
  staging:
    branch: master
    commands:
      - echo "Hello master"